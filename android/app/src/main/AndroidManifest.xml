<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Floating window (overlay) permission -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service for persistent execution -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network (LLM calls, IM channels, web search) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Storage (config, skills, scripts, sessions, memory) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Keep CPU awake during agent operation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Receive boot completed (auto-start agent) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".CrushClawApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Crush Claw"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrushClaw"
        android:usesCleartextTraffic="true">

        <!-- Main configuration activity (entry point) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service hosting the agent + floating windows -->
        <service
            android:name=".FloatingWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Configuration editor activity -->
        <activity
            android:name=".ConfigActivity"
            android:exported="false"
            android:label="Configuration"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustPan" />

        <!-- Channel configuration activity -->
        <activity
            android:name=".ChannelConfigActivity"
            android:exported="false"
            android:label="Channel Configuration"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustPan" />

        <!-- SAF DocumentsProvider: exposes .crush-claw/ in system file manager -->
        <provider
            android:name=".ClawDocumentsProvider"
            android:authorities="com.crushclaw.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!-- Boot receiver for auto-start -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
